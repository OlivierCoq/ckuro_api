'use strict';

/**
 * music-interface controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::music-interface.music-interface');
